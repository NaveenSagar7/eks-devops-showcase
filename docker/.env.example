# Copy to .env (gitignored) and fill in real values before running
# `docker compose up`. Defaults in docker-compose.yml only kick in if a
# variable is left unset entirely.
POSTGRES_DB=dailyjournal
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
APP_PAN_SECRET=change-me-too
